; Boosts PHP upload limits for the app (per-project .user.ini).
; Used by Apache (mod_php), FastCGI and PHP built-in server. Adjust php.ini instead if this file has no effect.
upload_max_filesize=20M
post_max_size=100M
max_execution_time=300
memory_limit=512M